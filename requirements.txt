# --- core framework -------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27                    # agent -> control plane, mTLS
cryptography>=43.0             # envelope encryption
pyarrow>=17.0                  # Arrow end-to-end + Parquet

# --- observability --------------------------------------------------------
opentelemetry-api>=1.27
opentelemetry-sdk>=1.27
prometheus-client>=0.21

# --- secrets --------------------------------------------------------------
hvac>=2.3                      # HashiCorp Vault
boto3>=1.35                    # AWS STS / KMS / S3

# --- source drivers (install only what a deployment actually uses) --------
psycopg[binary]>=3.2           # PostgreSQL + logical replication
# pymysql>=1.1                 # MySQL / MariaDB binlog
# oracledb>=2.4                # Oracle
# pyodbc>=5.1                  # SQL Server
# pymongo>=4.9                 # MongoDB change streams
# snowflake-connector-python>=3.12
# google-cloud-bigquery>=3.25
# google-cloud-bigquery-storage>=2.26
# redshift-connector>=2.1
# pyiceberg[glue,s3fs]>=0.7
# deltalake>=0.20
# adlfs>=2024.7                # Azure Data Lake
# gcsfs>=2024.9                # Google Cloud Storage
# msal>=1.31                   # Microsoft Graph, certificate credential
# google-api-python-client>=2.147
# google-auth>=2.35
# imapclient>=3.0
# confluent-kafka>=2.5

# --- development ----------------------------------------------------------
pytest>=8.3
pytest-cov>=5.0
graphviz>=0.20                 # diagram generation
